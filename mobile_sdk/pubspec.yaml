name: mobile_sdk
description: Flutter SDK for Robot OS Lite control.
version: 0.1.0
publish_to: "none"

environment:
  sdk: ">=3.5.0 <4.0.0"

dependencies:
  robot_protocol:
    path: ../protocol/dart
  flutter:
    sdk: flutter
  flutter_blue_plus: ^2.2.1
  mqtt_client: ^10.4.0

dev_dependencies:
  test: ^1.25.0
  flutter_test:
    sdk: flutter

