# Export Artifacts

This directory is the deployment bundle output.

Generated files:

- `lumi_kws.onnx`
- `tokens.txt`
- `keywords.txt`
- `config.yaml`
- `metadata.json`

Run `python3 scripts/export_onnx.py` after training to create the bundle.

