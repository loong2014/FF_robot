name: robot_protocol
description: Shared Robot OS Lite binary protocol for Dart and Flutter.
version: 0.1.0
publish_to: "none"

environment:
  sdk: ">=3.5.0 <4.0.0"

dev_dependencies:
  test: ^1.25.0

