[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"

[project]
name = "robot-protocol"
version = "0.1.0"
description = "Shared Robot OS Lite binary protocol for Python"
requires-python = ">=3.8"

[tool.setuptools.packages.find]
include = ["robot_protocol*"]

